import { CONTREG, CONTREG2, CONTREG3, CONTREG4, CONTREG5, CONTREG6} from "../types";

export default function signup(state = {}, action = {}) {
  switch(action.type){
    case CONTREG:
        break;
    case CONTREG2:
        break;
    case CONTREG3:
        break;
    case CONTREG4:
        break;
    case CONTREG5:
        break;
    case CONTREG6:
    default: 
      return state
  }
}